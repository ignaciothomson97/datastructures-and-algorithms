package cl.datastructuresandalgorithms.stringfirstindexocurrenceTODO;

public class StringFirstIndexOcurrence {
    public static void stringFirstIndexOcurrence(String haystack, String needle) {
        // Cambiar por SlidingWindow
        if (haystack.contains(needle)) {
            System.out.println(haystack.indexOf(needle));
        }
    }
}
